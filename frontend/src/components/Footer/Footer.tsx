import styles from './Footer.module.css';

const Footer = () => {
  return (
    <footer className={styles.footer}>
      <p className={styles.created_by}>Created by AY</p>
    </footer>
  );
};
export default Footer;
